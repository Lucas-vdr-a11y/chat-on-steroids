{
  "name": "chat-on-steroids-mcp-only",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "On-demand local files and explicitly selected MCP tools via the official Secure MCP Tunnel.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "verify": "npm test",
    "check": "node src/server.js --check"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "^3.25.76"
  }
}
